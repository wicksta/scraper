# PostgreSQL connection details
PGHOST=localhost
PGPORT=5432
PGDATABASE=planit
PGUSER=planit_user
PGPASSWORD=change_me

# Optional single-string connection URI (use instead of individual PG* vars if preferred)
# DATABASE_URL=postgres://planit_user:change_me@localhost:5432/planit
